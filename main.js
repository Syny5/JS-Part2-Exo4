function effacer()
{
  document.getElementById('infos').reset();
}
// L'idée est de réinitialiser toutes les valeurs écrites dans le formulaire
// C'est le .reset à la fin de l'ID du formulaire qui le permet
